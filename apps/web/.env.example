# Example env for apps/web
# Copy to .env.local or set in Vercel project settings

# Public API base URL used by client-side fetches and rewrites
NEXT_PUBLIC_API_URL=https://terra-data-api.vercel.app

# Optional
MAPBOX_ACCESS_TOKEN=
NASA_API_KEY=
