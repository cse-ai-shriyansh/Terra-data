{
  "name": "terra25",
  "version": "1.0.0",
  "description": "Terra25 — Animated Terra Data Portal for visualizing NASA Terra satellite datasets",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "prettier": "^3.0.0",
    "turbo": "latest"
  },
  "packageManager": "npm@10.2.4",
  "engines": {
    "node": ">=18"
  }
}